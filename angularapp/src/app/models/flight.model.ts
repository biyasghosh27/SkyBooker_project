interface Flight{
    flightId?:number;
    flightNumber:string;
    airline:string;
    departureLocation:string;
    arrivalLocation:string;
    departureTime:string;//change to string for now
    arrivalTime:string;//change to string for now
    price:number;
}